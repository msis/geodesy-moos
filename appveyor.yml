version: devel-{build}

platform:
    - x64
    - x86

environment:
    matrix:
    - BUILD_TYPE: cmake
      VS_VERSION: Visual Studio 11 2012
    
    - BUILD_TYPE: cmake
      VS_VERSION: Visual Studio 12 2013
      
    - BUILD_TYPE: cmake
      VS_VERSION: Visual Studio 14 2015

shallow_clone: true

init:
    - if "%platform%" == "x64" SET VS_FULL=%VS_VERSION% Win64
    - if "%platform%" == "x86" SET VS_FULL=%VS_VERSION%
    - cd %APPVEYOR_BUILD_FOLDER%/..
    - git clone -b 4.9.2 --depth=1 https://github.com/osgeo/proj.4 proj-4
    - cd proj-4
    - mkdir build
    - cd build 
    - cmake -G "%VS_FULL%" -DCMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE=../bin -DBUILD_TESTING=OFF -DBUILD_PROJ4_TESTS=OFF ../
    - cmake --build . --config Release
    - cmake --build . --config Release --target install
    - cd %APPVEYOR_BUILD_FOLDER%
    - mkdir build

build_script:
    - cd build
    - cmake -G "%VS_FULL%" -DPROJ4_DIR="c:/OSGeo4W/cmake/" -DCMAKE_BUILD_TYPE=$BUILD_TYPE ../
    - cmake --build . --config Release

on_success:
    - cmake --build . --config Release --target install

cache:
    - %APPVEYOR_BUILD_FOLDER%\..\proj-4 -> appveyor.yml
